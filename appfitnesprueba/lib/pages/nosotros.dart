import 'package:flutter/material.dart';
class Nosotros extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: Text('Quienes somos'),),
      body: Center(
        child: Text('nosotros somos una empresa privada'),
      ),
      
    );
  }
}